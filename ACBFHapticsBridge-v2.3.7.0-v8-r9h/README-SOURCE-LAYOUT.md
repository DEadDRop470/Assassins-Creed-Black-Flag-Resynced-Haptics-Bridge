# Source layout — v2.3.7.0 / v8-r9h

`src/main.cpp` is the exact source for the shipped r9h ASI.  
`build_linux_pe_r9h.sh` is the revision build script and `build_linux_pe.sh` points to the same current output.  
`audit_v8_r9h.py` performs the static PE/game-profile audit.

The compatibility-only source delta from v2.3.6 / r9g is documented in `CHANGELOG-v2.3.6-to-v2.3.7.0.md` and `V8-R9H-CHANGES.md`.

Historical r8/r9/r9b/r9c/r9d/r9e/r9f/r9g notes and scripts are retained only as revision reference where present.
