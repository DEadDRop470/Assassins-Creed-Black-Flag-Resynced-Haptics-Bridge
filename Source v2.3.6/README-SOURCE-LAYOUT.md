# Source layout — v2.3.6 / v8-r9g

`src/main.cpp` is the exact source for the shipped r9g ASI.  
`build_linux_pe_r9g.sh` is the revision build script and `build_linux_pe.sh` points to the same current output.  
`audit_v8_r9g.py` performs the static PE audit.

Historical r8/r9/r9b/r9c/r9d/r9e/r9f notes and scripts are retained only as revision reference where present.
