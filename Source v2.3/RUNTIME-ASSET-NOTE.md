# Runtime asset note

`ShadowsHapticsPack-v5.hfx` is a runtime waveform asset, not source code. It is included in the release ZIP because the ASI needs it at runtime. It is intentionally not described as source in the source package.
